<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="{{ site.lang }}">
      <title type="text">{{ site.title }}</title>
  <subtitle>{{ site.description | xml_escape }}</subtitle>
  <link
            rel="self" href="{{ page.url | absolute_url }}" />
  <link rel="alternate"
            href="{{ site.url | append: site.baseurl | default: '/' | absolute_url }}" />
  <updated>{{
      site.time | date_to_xmlschema }}</updated>
  <id>{{ site.url | append: site.baseurl | default:
            '/'
            | absolute_url }}</id> {% for author in site.authors %} <author>
            <name>{{ author.name }}</name>
            <email>{{ author.email }}</email>
      </author> {% endfor %} {% for post in site.posts
      limit: 10 %} <entry>
            <title type="html">{{ post.title | xml_escape }}</title>
      <link rel="alternate"
                  href="{{ post.url | absolute_url }}" />
      <published>{{ post.date | date_to_xmlschema
      }}</published>
      <updated>{{
                  post.last_modified_at | default: post.date | date_to_xmlschema }}</updated>
      <id>{{
      post.url |
                  absolute_url }}</id>
      <content type="html" xml:base="{{ post.url | absolute_url }}">{{
      post.content | html_escape }}</content>

      <author>
                  <name>{{ post.author | default: site.author }}</name>
            </author> {% for
      category in post.categories %} <category term="{{ category | xml_escape }}" /> {% endfor %} {%
      for tag in post.tags %} <category term="{{ tag | xml_escape }}" /> {% endfor %} </entry> {%
      endfor %} </feed>